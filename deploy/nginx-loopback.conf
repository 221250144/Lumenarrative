# Administrative preview through SSH forwarding; never bind this port publicly.
server {
    listen 127.0.0.1:8080;
    server_name localhost;
    server_tokens off;
    include /etc/nginx/snippets/xuguangji-app.conf;
}
