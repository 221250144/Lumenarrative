# 固定工作区、最短镜头与黑黄主题（2026-09-22，仅 main）

- 本机后端完整测试 125 项通过（17.30 秒），包含真实视频首尾/连续短镜头合并、0.5 秒精确边界、全片时间覆盖、旧版重分析升级与历史证据/关键帧保留。
- 模型请求参数回归确认：900 秒读取等待，0 可关闭读取等待，连接/上传/连接池仍有独立限制；读取超时不会自动重复发送请求。
- 杭州项目只读预览验证：28 个原镜头显示为 25 个合并镜头，时间连续覆盖 0–96.166667 秒，无独立不足 0.5 秒片段；全部原镜头 ID 能通过 `source_shot_ids` 找回。原诊断锚点仍指向原始证据范围。
- 前端构建通过。Safari 独立只读预览实测镜头列表滚到第 10 条、右栏滚到餐厅建议时两者互不影响，左栏和视频位置固定；章节 6 正确跳到镜头 24，镜头 6 证据回放显示原片香积寺画面。状态条展开高度有界，操作按钮保持可见；低于 700px 高或 1251px 宽时回到普通页面布局。
- 界面验收使用新前端和既有线上数据，视频直接读取生产媒体；未重新分析用户视频，未调用付费模型。
- 设计和兼容说明见 [工作区细节调整](WORKSPACE_REFINEMENTS.md)。

# 建议阅读体验修正（2026-09-22，仅 main）

- 本机与 Ubuntu 服务器后端完整测试均 103 项通过（分别 16.40 秒、39.22 秒）。新增回归覆盖原片时间替换、多个镜头和证据的不同范围、未知引用不编造时间、历史诊断与补拍清单直接读取生效、新素材验收说明、数据库原文与机器引用保持不变。
- 对“杭州一日游”既有分析做只读检查：按完整展示字段白名单统计，22 个字段含有的 50 次 UUID 引用均转换为源时间，发布后残留为 0；其中建议正文为 14 个字段、32 次引用。除展示文案外，全部 JSON 字段与发布前一致；未重新分析或产生模型调用。
- 前端 TypeScript 与生产构建通过。独立 Safari 标签使用线上数据的本地只读预览，从首条建议滚动至末条及分析范围，视频位置保持不变，右栏标题、标签、底部清单按钮始终可见；“审看依据”正文同样可独立滚动。
- 三栏桌面布局使用独立滚动区域；窄屏保留页面自然滚动。本次未重新执行完整移动端兼容性验收。
- 新模型提示要求描述使用时间段；API 在读取历史数据时转换内部编号，结构化 ID 和原始验收条件仍保留供系统关联、回看和验证使用。不改写历史记录，不要求用户重新分析。
- 主分支 `3c7b27e` 已发布到 `/opt/xuguangji/releases/review-ui-20260922`。公网新 JS/CSS 与本地构建 SHA256 一致；页面、健康检查、项目接口为 HTTP 200，无认证与 HTTPS 跳转；视频 Range 为 206。实验分支未改动。

# Vlog v2 验证补充（2026-09-22）

并发升级补充：本机与服务器均 **89 项测试通过**，包括线程/进程共享上限、同素材预处理写入隔离、异常释放、乱序合并与缓存。服务器 8 镜头实际百炼请求对照：串行 32.324 秒，并发 5.340 秒，HTTP 请求峰值由 1 提升到 8，两轮各 8 次请求、零失败、无应用证据缓存；仅测视觉提取阶段。详见 [8 核并发设计](CONCURRENCY.md)。

公网串联流程另用循环合成片验证：预处理识别 9 个镜头，全片审阅引用错误在修复实现上线后重试成功，并复用了已完成的视觉缓存；4 条建议均符合最多两段证据限制，导出 8.08 秒 MP4，HTTP 200 / Range 206。该次缓存重试不纳入上述串行/并发测速。模型引用修复另有 5 项定向自动化测试。

- 本机与 Ubuntu 服务器后端均为 66 项通过，覆盖真实切点/短镜头/VFR/同镜头采样、双证据去重、引用越界拒绝、全片缺失判断覆盖保护、具体计划、主片与补充素材隔离、缓存与历史保护。
- 36 秒主片、30 秒目标的回归确认默认 EDL 保留 36 秒全长；更换主片后旧任务验收返回 409。
- 前端 TypeScript 与生产构建通过。浏览器验证镜头范围播放、旧意见最多两证据、390px 宽度无横向溢出。
- 独立测试库中实际调用百炼：三段合成视频切成 3 个镜头，`qwen3-vl-plus` 三次视觉理解与 `qwen-plus` 全片审阅成功；模型没有强行给测试色块编造补拍建议。导出 MP4 3 秒，HTTP Range 为 206。
- 合成视频仅验证算法和链路；具体建议的结构、验收与引用规则由明确的测试观察验证。尚未建立实拍 Vlog 语义准确率评测集。
- 服务器新版经公网 HTTP 实测：上传、Celery 预处理、3 个镜头、百炼真实分析、MP4 导出全部成功。页面 HTTP 200，视频 Range 206；FFprobe 检出 H.264 + AAC、3.021 秒；无 HTTPS 重定向或登录挑战。
- 发布目录 `/opt/xuguangji/releases/vlog-v2-20260922-1`，切换前备份 PostgreSQL 与媒体，保留已有项目与历史分析。

# 旭光集 v0.1 验证记录

日期：2026-09-22。环境：macOS、Python 3.12.14、Node 24.19.0、FFmpeg 9.0.2、SQLite、本地单 worker。

## 已实际执行

- 后端：`PYTHONPATH=backend .venv/bin/pytest backend/tests -q`，17 项通过。
- 前端：`npm --prefix frontend run build`，TypeScript 检查与 Vite 生产构建通过。
- 全新临时数据库：Alembic `upgrade head` 成功，版本为 `0001`。没有在用户数据库执行降级。
- Compose：通过 YAML 结构检查，包含数据库、Redis、迁移、API、worker、Web 六个服务；没有实际启动 Docker 容器。
- 浏览器：从首页创建演示项目，等待两个视频预处理，开始诊断并确认缺少过程镜头。
- 浏览器：生成修改前 MP4；生成补全计划；提交无关镜头后显示“未通过验收”；提交正确镜头后显示“通过验收”。
- 浏览器：重新诊断显示“当前没有必要补充的镜头”，生成第二版粗剪。原版约 10.02 秒，新版约 15.02 秒；两个 video 元素均 `readyState=4`，无解码错误。第二版包含场景、过程、结果三条素材，没有自动加入未通过验收的补充片段。
- 浏览器控制台：该验证页未记录 error 日志。已人工检查首页与版本对比页布局。

## 百炼真实接入测试

- 本地 `.env` 已切换为 `MODEL_PROVIDER=qwen`，使用控制台给出的北京地域业务空间专属 OpenAI-compatible 地址。密钥仅保存在被 Git 忽略的本地 `.env`，文件权限为 `0600`。
- `qwen-plus` 与 `qwen3-vl-plus` 均实际返回 HTTP 200；应用 Provider 的需求输出和图片证据均通过 Pydantic 校验，证据时间通过源时间边界校验。
- 针对本机 SOCKS 代理环境补充 `httpx[socks]` 依赖并更新锁文件；更新后重新执行后端测试，17 项通过。
- 重启本地 API 后 `/api/v1/health` 返回 `provider=qwen`、`model_configured=true`；前端 HTTP 200。
- 通过运行中的 API 创建“千问接入测试 · 纯红画面”，上传 FFmpeg 生成的 3 秒纯红视频。预处理与分析作业均为 `succeeded`，产生 5 条需求、1 条真实视觉证据、5 条匹配和 2 条可选待复核建议。失败采样区间为空，证据记录 `provider=qwen`、`model=qwen3-vl-plus`、`demo=false`。
- 本次额外调用只发送程序生成的纯色图片/视频采样帧和测试文字。结果仅证明接入与处理流程可用，不代表实拍叙事准确率；模型仍可能输出多余建议或英文说明，需要继续调优与人工复核。

## 自动化测试覆盖

2026-09-22 补充：Ubuntu 服务器部署后，Linux 测试同样 17 项通过；实际 PostgreSQL + Redis + Celery 环境跑通真实模型分析与 MP4 导出。公网 HTTP 免登录访问经系统代理返回 200，Edge 已实际打开；补齐 HTTP 环境的 UUID fallback 并通过针对性校验和前端生产构建，详见 `SERVER_DEPLOYMENT.md`。

- 源时间合法性、重叠分段、分离时间的同一动作不会误合并。
- 不存在的证据 ID、错误的证据类型、重复需求结果会被拒绝。
- 部分覆盖与音频未分析时保留不确定性；未确认模型建议不升级为必要缺口。
- 预算边际收益去重、零预算、待确认任务不自动推荐。
- EDL 边界、总时长与项目素材引用、文件路径边界。
- 中文文件名真实上传、上传幂等、HTTP Range 预览、无音轨、非法伪视频、实际 MP4 导出与 FFprobe 复验。
- 完整故事、缺失过程、动作遮挡、素材乱序、氛围混剪、对白交代、属性冲突、部分分析失败八种夹具。
- 无关补充不关闭缺口，正确补充逐项通过，失败提交不自动进入粗剪。
- 旧分析与旧验证结果不覆盖新的项目 revision。
- 人工需求修改、缺口忽略状态在重新分析后保留。
- 缺少真实凭据时明确失败，不退回 mock；切换配置后不能混用旧分析任务。
- 可变帧率和带音轨视频，ASR 未配置状态，初稿默认禁止自定义重排。
- 并发使用相同分析幂等键只产生一个分析。
- Provider HTTP 模拟：结构错误只定向修复一次，重复错误失败，鉴权失败不泄露 key/原始响应。

## 验证边界

自动化测试中的语义结果来自固定夹具或 HTTP 模拟响应；真实 VLM/LLM 接入测试另列于上节。未执行真实 ASR 调用，未验证影石 SDK。没有真实用户调研与实拍标注集，不报告准确率。

测试运行有两个来自 Starlette/httpx/AnyIO 的依赖弃用提示；当前检查正常通过，后续依赖升级时调整测试客户端。

人工界面验证使用当前应用内浏览器视口；没有宣称已完成全部移动端、浏览器或无障碍兼容性测试。
